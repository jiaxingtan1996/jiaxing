---
import '../styles/global.css';

interface Props {
  title?: string;
  description?: string;
  currentPath?: string;
}

const {
  title = 'Jiaxing Tan',
  description = 'Notes on machine learning and references.',
  currentPath = '/'
} = Astro.props;

const isActive = (path: string) => currentPath === path;
---
<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />
    <meta name="description" content={description} />
    <title>{title}</title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&family=JetBrains+Mono:wght@400;500;600&display=swap" rel="stylesheet">
  </head>
  <body>
    <main class="container">
      <header class="site-header">
        <a href="/" style="color: inherit; text-decoration: none;">
          <h1 class="site-title">Jiaxing Tan-Staying focused when voices spread outside.</h1>
        </a>
        <p class="site-subtitle">cryo-EM · machine learning · references</p>
        <nav class="nav" aria-label="Primary">
          <a href="/" class={isActive('/') ? 'active' : ''}>Home</a>
          <a href="/cryo-EM/" class={isActive('/cryo-EM/') ? 'active' : ''}>Cryo-EM</a>
          <a href="/machine-learning/" class={isActive('/machine-learning/') ? 'active' : ''}>Machine Learning</a>
          <a href="/references/" class={isActive('/references/') ? 'active' : ''}>References</a>
        </nav>
      </header>
      <slot />
      <footer class="footer">
        © 2026 Jiaxing Tan. All rights reserved.
      </footer>
    </main>
  </body>
</html>
